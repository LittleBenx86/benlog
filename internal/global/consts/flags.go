package consts

const (
	PREFIX_EVENT_DESTROY = "Destroy_" // The event prefix that needs to be destroyed when Benlog application exits.
	PREFIX_CONFIG_KEY    = "Config_"  // The cache prefix that those configurations need to be cached.
	USER_ANONYMOUS_NAME  = "anonymous"
)
